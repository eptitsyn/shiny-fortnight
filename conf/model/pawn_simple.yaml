num_metrics: 3

num_hidden_features: 128
num_hidden_layers: 1
gate_nn_num_layers: null
num_gates: null
activation: gelu
norm_type: layer
residual: true

concat_consecutive_hidden_states: false
pos_embed_dim: 16
max_len: ${data.max_len}

aggregation_method: attention

dropout: 0.1
dropout_tokens: 0.0
